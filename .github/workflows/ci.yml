name: CI

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pull-requests: read

jobs:
  run:
    uses: borch-ai/.github/.github/workflows/go-ci-standalone.yml@fix/go-ci-auth
    secrets: inherit
